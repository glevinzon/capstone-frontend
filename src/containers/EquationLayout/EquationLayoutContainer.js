import {connect} from 'react-redux'
import EquationLayout from 'layouts/EquationLayout/EquationLayout'

let mapActionCreators = {
}
let mapStateToProps = (state) => ({
})

export default connect(mapStateToProps, mapActionCreators)(EquationLayout)
